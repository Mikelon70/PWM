/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CajanoticiasComponent } from './cajanoticias.component';

describe('CajanoticiasComponent', () => {
  let component: CajanoticiasComponent;
  let fixture: ComponentFixture<CajanoticiasComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CajanoticiasComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CajanoticiasComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
